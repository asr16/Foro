{% extends 'base.html.twig' %}

{% block title %}Hilo{% endblock %}

{% block body %}
    <h1>Hilo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hilo.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ hilo.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ hilo.text }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ hilo.date ? hilo.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Visible</th>
                <td>{{ hilo.visible }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hilo_index') }}">back to list</a>

    <a href="{{ path('hilo_edit', {'id': hilo.id}) }}">edit</a>

    {{ include('hilo/_delete_form.html.twig') }}
{% endblock %}
