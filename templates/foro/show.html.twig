{% extends 'base.html.twig' %}

{% block title %}Foro{% endblock %}

{% block body %}
    <h1>Foro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ foro.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ foro.Title }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ foro.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('foro_index') }}">back to list</a>

    <a href="{{ path('foro_edit', {'id': foro.id}) }}">edit</a>

    {{ include('foro/_delete_form.html.twig') }}
{% endblock %}
